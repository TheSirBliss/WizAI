/** @type {import('next').NextConfig} */
const nextConfig = {
  // Questa configurazione risolve i problemi di memoria su Vercel
  experimental: {
    workerThreads: false,
    cpus: 1
  },
};

export default nextConfig;
