/** @type {import('next').NextConfig} */
const nextConfig = {
  // Disabilita il compilatore SWC, forzando l'uso di Babel
  swcMinify: false,

  productionBrowserSourceMaps: false,
  experimental: {
    workerThreads: false,
    cpus: 1
  },
};

export default nextConfig;
